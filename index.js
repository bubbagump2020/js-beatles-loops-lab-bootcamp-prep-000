// add solution here
function theBeatlesPlay(musicians, instruments){
  var array3 = []
  for(let i = 0; i < array1.length; i++){
    var beatleList = `${i} plays ${instruents[i]}`
    array3.push(beatleList)
  }
}