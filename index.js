// add solution here
function theBeatlesPlay(musicians, instruments){
  var array = []
  for(let i = 0; i < array1.length; i++){
    array[i] = `${i} plays ${instruents[i]}`
    array.push(beatleList)
  }
}